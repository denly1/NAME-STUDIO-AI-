# 🤖 Автономный AI-Агент в NAME STUDIO AI

## Обзор системы

NAME STUDIO AI теперь включает полноценную систему автономного AI-агента, способного самостоятельно исследовать код, планировать и выполнять сложные задачи.

## Архитектура

### 1. Agent Harness (`agentHarness.ts`)
**Координатор агента** - управляет выполнением задач, инструментами и диалогом.

**Основные функции:**
- Создание плана выполнения задачи
- Координация инструментов
- Управление контекстом разговора
- Отслеживание прогресса выполнения

**Пример использования:**
```typescript
import { agentHarness } from './services/agentHarness';

// Установить рабочую директорию
agentHarness.setWorkspaceRoot('/path/to/project');

// Выполнить задачу
const task = await agentHarness.executeTask(
  'Добавь валидацию email в форму регистрации',
  (step) => console.log('Шаг:', step.description),
  (plan) => console.log('План создан:', plan),
  (diff) => console.log('Изменение готово:', diff.file)
);
```

### 2. Agent Tools (`agentTools.ts`)
**Набор инструментов** для работы с файловой системой и кодом.

**Доступные инструменты:**

| Инструмент | Назначение | Параметры |
|------------|-----------|-----------|
| `readFile` | Чтение файла | `path: string` |
| `listDirectory` | Список файлов в папке | `dirPath: string` |
| `searchFiles` | Поиск файлов по имени | `pattern: string, directory?: string` |
| `searchContent` | Поиск по содержимому | `query: string, directory?: string` |
| `createFileDiff` | Создание diff для изменений | `filePath: string, newContent: string` |
| `applyEdit` | Применение изменений | `filePath: string, newContent: string` |
| `createFile` | Создание нового файла | `filePath: string, content: string` |
| `deleteFile` | Удаление файла | `filePath: string` |
| `executeCommand` | Выполнение команды | `command: string, cwd?: string` |

**Пример:**
```typescript
import { agentTools } from './services/agentTools';

// Поиск файлов
const result = await agentTools.searchFiles('*.tsx', '/src/components');

// Чтение файла
const fileResult = await agentTools.readFile('/src/App.tsx');

// Создание diff
const diffResult = await agentTools.createFileDiff(
  '/src/components/Form.tsx',
  newContent
);
```

### 3. Codebase Index (`codebaseIndex.ts`)
**Семантический поиск** по кодовой базе с индексацией.

**Возможности:**
- Индексация проекта
- Семантический поиск
- Поиск определений (функций, классов)
- Поиск использований символов
- Анализ структуры файлов

**Пример:**
```typescript
import { codebaseIndex } from './services/codebaseIndex';

// Индексировать проект
await codebaseIndex.indexProject(allFiles);

// Семантический поиск
const results = await codebaseIndex.search('authentication logic', 10);

// Найти определение
const definitions = await codebaseIndex.findDefinition('LoginForm');

// Найти использования
const usages = await codebaseIndex.findUsages('useAuth');
```

### 4. Advanced Diff Viewer (`AdvancedDiffViewer.tsx`)
**Интерактивный просмотр изменений** с возможностью принятия/отклонения.

**Функции:**
- Боковая панель со списком измененных файлов
- Цветовая подсветка изменений (зеленый +, красный -)
- Кнопки Accept/Reject для каждого файла
- Accept All / Reject All для всех изменений
- Статистика изменений

**Интерфейс:**
```typescript
<AdvancedDiffViewer
  diffs={fileDiffs}
  onAccept={(diff) => applyDiff(diff)}
  onReject={(diff) => rejectDiff(diff)}
  onAcceptAll={() => applyAllDiffs()}
  onRejectAll={() => rejectAllDiffs()}
/>
```

## Рабочий процесс

### Шаг 1: Пользователь дает задачу
```
"Добавь валидацию email в форму регистрации"
```

### Шаг 2: Агент создает план
```
1. Найти файл формы регистрации
2. Прочитать текущий код формы
3. Добавить функцию валидации email
4. Обновить обработчик отправки формы
5. Добавить отображение ошибок валидации
```

### Шаг 3: Агент исследует код
- Использует `searchFiles` для поиска формы
- Использует `codebaseIndex.search` для поиска связанного кода
- Читает найденные файлы с помощью `readFile`

### Шаг 4: Агент выполняет изменения
- Создает diff'ы для каждого измененного файла
- Показывает изменения в `AdvancedDiffViewer`

### Шаг 5: Пользователь проверяет и применяет
- Просматривает каждое изменение
- Нажимает Accept для применения
- Или Reject для отклонения

## Типы данных

### AgentTask
```typescript
interface AgentTask {
  id: string;
  description: string;
  plan?: AgentPlan;
  steps: AgentStep[];
  diffs: FileDiff[];
  status: 'planning' | 'executing' | 'waiting_approval' | 'completed' | 'failed';
  error?: string;
}
```

### AgentStep
```typescript
interface AgentStep {
  id: string;
  type: 'thinking' | 'tool_use' | 'result' | 'error';
  description: string;
  tool?: string;
  input?: any;
  output?: any;
  timestamp: number;
}
```

### AgentPlan
```typescript
interface AgentPlan {
  goal: string;
  steps: string[];
  currentStep: number;
  completed: boolean;
}
```

### FileDiff
```typescript
interface FileDiff {
  file: string;
  oldContent: string;
  newContent: string;
  diff: string;
}
```

## Режимы работы

### Автономный режим (Autonomous Mode)
Агент работает полностью самостоятельно:
- Сам планирует
- Сам исследует код
- Сам создает изменения
- Показывает diff'ы для утверждения

### Ручной режим (Manual Mode)
Агент работает под контролем:
- Показывает план перед выполнением
- Запрашивает подтверждение на каждом шаге
- Требует явного утверждения изменений

## Системные промпты

### Planning Prompt
Создает детальный план выполнения задачи:
- Разбивает задачу на шаги
- Учитывает структуру проекта
- Планирует чтение файлов перед редактированием
- Планирует поиск релевантного кода

### Execution Prompt
Выполняет конкретный шаг плана:
- Использует доступные инструменты
- Получает контекст из codebase index
- Создает минимальные, точные изменения
- Сохраняет стиль кода

## Интеграция в UI

### В AIPanel добавлено:
1. **Кнопка "Autonomous Mode"** - включение автономного режима
2. **Отображение плана** - показ шагов выполнения
3. **Отображение прогресса** - текущий шаг и статус
4. **Advanced Diff Viewer** - просмотр и утверждение изменений
5. **История шагов** - лог действий агента

### Quick Actions обновлены:
- "Fix Error" - автономное исправление ошибок
- "Refactor" - автономный рефакторинг
- "Generate Tests" - автономная генерация тестов
- "Analyze" - автономный анализ кода

## Примеры использования

### Пример 1: Добавление функции
```typescript
// Пользователь: "Добавь функцию для валидации телефона"

// Агент:
// 1. Ищет файлы с валидацией
// 2. Читает существующие валидаторы
// 3. Создает новую функцию validatePhone
// 4. Добавляет тесты
// 5. Показывает diff'ы
```

### Пример 2: Исправление бага
```typescript
// Пользователь: "Исправь ошибку TypeError в LoginForm"

// Агент:
// 1. Находит файл LoginForm
// 2. Анализирует код
// 3. Находит источник ошибки
// 4. Создает исправление
// 5. Показывает diff
```

### Пример 3: Рефакторинг
```typescript
// Пользователь: "Вынеси логику аутентификации в отдельный хук"

// Агент:
// 1. Находит код аутентификации
// 2. Создает новый файл useAuth.ts
// 3. Переносит логику в хук
// 4. Обновляет компоненты для использования хука
// 5. Показывает все diff'ы
```

## Преимущества системы

### 🎯 Контроль
- Все изменения проверяются перед применением
- Прозрачный процесс выполнения
- Возможность отменить любое изменение

### 🚀 Автономность
- Агент сам планирует и исследует
- Не требует постоянного надзора
- Выполняет сложные многошаговые задачи

### 🔍 Контекстное понимание
- Семантический поиск по коду
- Понимание структуры проекта
- Учет существующих паттернов

### 📊 Прозрачность
- Показывает план перед выполнением
- Логирует каждый шаг
- Объясняет принятые решения

### 🛡️ Безопасность
- Не применяет изменения автоматически
- Показывает diff'ы для проверки
- Можно отклонить любое изменение

## Будущие улучшения

- [ ] Интеграция с Git для автоматических коммитов
- [ ] Поддержка веб-поиска для актуальной документации
- [ ] MCP интеграция для внешних сервисов
- [ ] Векторная база данных для более точного поиска
- [ ] Кэширование индекса проекта
- [ ] Поддержка множественных языков программирования
- [ ] Автоматическое тестирование изменений
- [ ] Интеграция с CI/CD

## Заключение

Система автономного AI-агента в NAME STUDIO AI предоставляет мощный инструмент для разработки, сочетающий автономность с контролем, прозрачность с эффективностью.

**Ключевые принципы:**
1. **Агент помогает, не мешает**
2. **Контроль всегда у разработчика**
3. **Прозрачность на каждом шаге**
4. **Минимальные, точные изменения**
5. **Обучение через наблюдение**

---

**NAME STUDIO AI** - Your Premium AI-Powered Development Studio 🚀
