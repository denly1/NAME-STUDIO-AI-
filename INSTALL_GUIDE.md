# 🚀 NeuroDesk IDE - Установка и запуск

## Ваше собственное приложение как Cursor AI!

Это **полностью самостоятельное Electron приложение** с Monaco Editor (движок VS Code) и AI-агентами.

---

## 📦 Установка

### Шаг 1: Установите зависимости

```powershell
cd "C:\Users\Sasha\Desktop\NAME IDE\neurodesk-ide"
npm install
```

### Шаг 2: Запустите приложение

```powershell
npm run dev
```

Приложение откроется в отдельном окне!

---

## 🎯 Что это такое?

### Полноценная IDE с:

✅ **Monaco Editor** - тот же редактор, что в VS Code  
✅ **Файловый менеджер** - открытие папок, создание файлов  
✅ **AI-агенты** - помощь с кодом (нужен API ключ)  
✅ **Терминал** - встроенный (в разработке)  
✅ **Подсветка синтаксиса** - для всех языков  
✅ **Множественные вкладки** - работа с несколькими файлами  

### Это ВАШЕ приложение:

- 🔧 **Полный контроль** - весь исходный код у вас
- 🎨 **Кастомизация** - меняйте что угодно
- 📦 **Standalone** - работает как отдельная программа
- 🚀 **Расширяемость** - добавляйте свои функции

---

## 🛠️ Структура проекта

```
neurodesk-ide/
├── electron/              # Electron backend
│   ├── main.ts           # Главный процесс
│   └── preload.cjs       # Preload script
├── src/                  # React frontend
│   ├── components/       # UI компоненты
│   │   ├── TitleBar.tsx      # Верхняя панель
│   │   ├── Sidebar.tsx       # Файловое дерево
│   │   ├── EditorPanel.tsx   # Monaco Editor
│   │   ├── Terminal.tsx      # Терминал
│   │   └── AIPanel.tsx       # AI чат
│   ├── store/           # Zustand state
│   ├── types.ts         # TypeScript типы
│   ├── App.tsx          # Главный компонент
│   └── main.tsx         # Entry point
├── package.json         # Зависимости
└── README.md           # Документация
```

---

## 💻 Использование

### Открыть папку

1. Нажмите кнопку "Open Folder" в сайдбаре
2. Выберите папку с вашим проектом
3. Файлы появятся в дереве слева

### Редактировать файл

1. Кликните на файл в дереве
2. Файл откроется в Monaco Editor
3. Редактируйте код с подсветкой синтаксиса
4. `Ctrl+S` для сохранения

### AI помощь

1. Откройте AI панель справа
2. Задайте вопрос о коде
3. AI ответит (требуется API ключ от OpenRouter)

---

## 🔑 Настройка AI (опционально)

Чтобы использовать AI-агентов:

1. Получите API ключ на [openrouter.ai](https://openrouter.ai)
2. Добавьте в код (файл `src/components/AIPanel.tsx`):

```typescript
// Замените mockAIResponse на реальный вызов API
const response = await fetch('https://openrouter.ai/api/v1/chat/completions', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer YOUR_API_KEY`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'openai/gpt-3.5-turbo',
    messages: [{ role: 'user', content: input }]
  })
});
```

---

## 📦 Сборка .exe файла

Когда приложение готово, создайте установщик:

```powershell
npm run build:win
```

Это создаст `.exe` файл в папке `release/`

---

## 🎨 Кастомизация

### Изменить тему

Отредактируйте цвета в `src/index.css` и компонентах

### Добавить функции

1. Создайте новый компонент в `src/components/`
2. Добавьте в `src/App.tsx`
3. Добавьте IPC handlers в `electron/main.ts` если нужно

### Добавить AI агентов

Отредактируйте `src/components/AIPanel.tsx` - добавьте своих агентов

---

## 🐛 Troubleshooting

### Приложение не запускается

```powershell
# Удалите node_modules и переустановите
rm -r node_modules
npm install
npm run dev
```

### Ошибки компиляции TypeScript

Это нормально до установки зависимостей. После `npm install` всё заработает.

### Vite сервер не запускается

Убедитесь что порт 5173 свободен:

```powershell
netstat -ano | findstr :5173
```

---

## 🚀 Следующие шаги

1. ✅ Установите зависимости: `npm install`
2. ✅ Запустите: `npm run dev`
3. ✅ Откройте папку с проектом
4. ✅ Начните редактировать код!
5. 🔧 Добавьте свои функции
6. 📦 Соберите .exe: `npm run build:win`

---

## 💡 Идеи для расширения

- 🔌 **Плагины** - система расширений
- 🎨 **Темы** - кастомные цветовые схемы
- 🔍 **Поиск** - глобальный поиск по проекту
- 🐛 **Отладчик** - интеграция debugger
- 📊 **Git** - интеграция системы контроля версий
- 🤖 **Больше AI** - автодополнение, рефакторинг
- 💬 **Чат** - командная работа
- 🌐 **Расширения** - marketplace

---

## ✅ Готово!

Теперь у вас есть **собственная IDE**, которую вы можете развивать как хотите!

Это ваше приложение - делайте с ним что угодно! 🎉
